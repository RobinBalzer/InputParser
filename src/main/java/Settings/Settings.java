package Settings;

public class Settings {

     public static String inputFileDirectory = "src/main/resources/input/"; // use this for Intellij runs
    //public static String inputFileDirectory = "resources/input/"; // use this for building the .jar

    // input/output from the approximateQueries point of view. (we create the input for approximateQueries.
    // -> we store the output of InputParser in input (for approximateQueries)

    public static String outputFileDirectory = "src/main/resources/output/"; // use this for Intellij runs
    //public static String outputFileDirectory = "resources/inputParserResults/"; // use this for building the .jar
}

